# Drainage Management

Categories: `DRAIN_BLOCKAGE`, `WATER_STAGNATION`.

Category-only module, same pattern as `modules/civic/` -- no dedicated
backend code, uses the core complaint engine directly.

# Flutter's default rules (proguard-android-optimize.txt) cover the
# framework itself. Add module-specific keep rules here only if a release
# build breaks something that works in debug -- start with none rather
# than pre-emptively disabling optimization.
